import 'package:flutter/material.dart';

class crud_test extends StatefulWidget {
  const crud_test({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Page'),
      ),
      body: const Center(
        child: Text('Hello from MyPage!'),
      ),
    );
  }
}
